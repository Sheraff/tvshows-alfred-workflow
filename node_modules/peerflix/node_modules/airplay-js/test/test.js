var mdns = require('../airplay');

var browser = new mdns.createBrowser(mdns.tcp('airplay'));

browser.on('ready', function () {
  console.log('ready');
  browser.discover();
});

browser.on('update', function (data) {
  console.log('data:', data);
});
